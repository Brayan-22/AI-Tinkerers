# Mercadia · todas las llaves en un solo lado.
# Cómo sacar cada una: README, sección "Variables de entorno".
# Sin una llave, esa pieza se apaga sola y el mercado sigue cerrando tratos.
# Comprueba con: npm run estado

# ── Cerebro · sin esto la negociación es determinista ────────────────────────
# DeepInfra primero, OpenRouter de respaldo. Modelos por defecto medidos con el
# prompt real: DEEPINFRA_MODEL y LLM_MODEL los cambian.
DEEPINFRA_API_KEY=
OPENROUTER_API_KEY=

# ── Canales · el comprador en Slack, el proveedor en su celular ──────────────
# Crea la app pegando deploy/slack-app-manifest.yml, y valida: npm run slack:check
SLACK_APP_TOKEN=
SLACK_BOT_TOKEN=
TELEGRAM_BOT_TOKEN=

# ── Catálogo · el mercado arranca vacío: los proveedores se registran solos ──
# CATALOGO_DEMO=1 mete nueve proveedores inventados, marcados como utilería en
# la pantalla. Útil para enseñar el flujo sin nadie del otro lado.
CATALOGO_DEMO=

# ── Descubrimiento · proveedores reales de la web y precio de referencia ─────
EXA_API_KEY=
MARKET_PLACE=Colombia

# ── Registro · el rastro de cada compra en el workspace del agente ───────────
# npx ambiguous auth signup la deja en .ambi/config.json; cópiala acá para que
# el servidor no necesite ese archivo.
AMBIGUOUS_API_KEY=

# ── Correo · sin dominio verificado solo sirve onboarding@resend.dev ─────────
MAIL_API_KEY=
MAIL_FROM=onboarding@resend.dev

# ── Cadena · opcional, falla suave ───────────────────────────────────────────
# Las llaves de los agentes de la casa en una línea (npm run wallets crea
# wallets.json; pega su contenido acá). Sin esto los tratos cierran igual, con
# llaves en custodia del mercado.
WALLETS_JSON=
BASE_SEPOLIA_RPC=https://sepolia.base.org

# ── Servidor ─────────────────────────────────────────────────────────────────
PORT=3000
PUBLIC_URL=
# CONTROL_TOKEN · OBLIGATORIO si la URL es pública, o cualquiera aprueba
# compras ajenas. La pantalla de operación entra por /arena?t=EL_TOKEN
CONTROL_TOKEN=
APPROVAL_SECRET=
